'use client';

import React from 'react';
import UnifiedRoutePlanner from '@/components/UnifiedRoutePlanner';

export default function RouteSimulatorPage() {
  return <UnifiedRoutePlanner initialMode="manual" />;
}
