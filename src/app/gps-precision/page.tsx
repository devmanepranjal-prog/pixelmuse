'use client';

import React from 'react';
import UnifiedRoutePlanner from '@/components/UnifiedRoutePlanner';

export default function GpsPrecisionPage() {
  return <UnifiedRoutePlanner initialMode="gps" />;
}
